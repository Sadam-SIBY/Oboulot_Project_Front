// Imports custom hooks for accessing Redux store state and dispatch function
import { useAppSelector, useAppDispatch } from "./redux";

// Exports the custom hooks for use in components
export { useAppSelector, useAppDispatch };
